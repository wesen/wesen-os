These workflows are intentionally disabled for `wesen-os`.

Reason:
- `wesen-os` is currently acting as a bundling/integration repo.
- CI, release, and scanning should run in the owned component repos instead.

To re-enable a workflow later, move the desired file back into `.github/workflows/`.
