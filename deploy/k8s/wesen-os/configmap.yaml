apiVersion: v1
kind: ConfigMap
metadata:
  name: wesen-os-config
data:
  profiles.runtime.yaml: |
    slug: cluster-default
    profiles:
      default:
        slug: default
        runtime:
          step_settings_patch:
            ai-chat:
              ai-engine: gpt-4.1-mini
  federation.registry.json: |
    {
      "version": 1,
      "remotes": [
        {
          "remoteId": "inventory",
          "mode": "remote-manifest",
          "enabled": false,
          "manifestUrl": "https://assets.example.invalid/remotes/inventory/versions/latest/mf-manifest.json"
        }
      ]
    }
