# Tasks

## TODO

- [x] Reproduce startup failure in `wesen-os` with default operator environment.
- [x] Trace failure path from launcher command bootstrap to runtime YAML decode.
- [x] Confirm fallback behavior and precedence in `geppetto` and `pinocchio` docs/tests.
- [x] Capture immediate operator workaround commands.
- [x] Produce detailed bug report + source-level implementation research design.
- [x] Produce investigation diary with command-level evidence.
- [x] Reproduce and document icon/context-menu override behavior with Playwright against `pnpm dev`.
- [x] Implement context-menu propagation fix so app/widget menus are not overridden by window-level menu.
- [x] Run `docmgr doctor` for ticket hygiene.
- [ ] Implement source fix in `geppetto` (configurable fallback policy + better error context).
- [ ] Propagate fix to `pinocchio` and `wesen-os` launch paths; add regression tests.
