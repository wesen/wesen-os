# Host Image Tagging Plan

Generated by:

- `ttmp/2026/03/29/DEPLOY-001--k3s-host-deployment-federated-modules-and-github-ci-cd/scripts/06-resolve-host-image-tags.sh`

## Repository Inputs

- origin: `git@github.com:wesen/wesen-os`
- repo_slug: `wesen/wesen-os`
- branch: `task/os-openai-app-server`
- git_sha: `ee654361f93aadb571aca13f14a595268905f117`

## Decision

- canonical image repository: `ghcr.io/wesen/wesen-os`
- immutable publish tag: `ghcr.io/wesen/wesen-os:sha-ee654361f93aadb571aca13f14a595268905f117`
- short immutable convenience tag: `ghcr.io/wesen/wesen-os:sha-ee654361f93a`
- mutable branch tag:
  - none (non-main branch)
- deployment manifests should pin image digests rather than mutable tags

## Proposed OCI Labels

- `org.opencontainers.image.source=https://github.com/wesen/wesen-os`
- `org.opencontainers.image.revision=ee654361f93aadb571aca13f14a595268905f117`
- `org.opencontainers.image.title=wesen-os`

## Notes

- The immutable tag should always exist for traceability and rollback.
- The short SHA tag is optional but convenient for humans reading registry listings.
- The mutable `main` tag should exist only for the default branch so staging or ad hoc branches do not overwrite a shared moving reference.
- Kubernetes manifests should ultimately deploy `ghcr.io/wesen/wesen-os@sha256:...` after the GHCR workflow resolves the pushed digest.
- Non-main branches can still publish preview images if needed, but they should use explicit branch-scoped tags rather than reusing `main`.

## Suggested Preview Tag

- `ghcr.io/wesen/wesen-os:branch-task-os-openai-app-server-ee654361f93a`
